[project]
name = "inoc-eval"
version = "0.1.0"
description = "Evaluation pipeline for self-inoculation experiments"
requires-python = ">=3.10"
dependencies = [
    "inspect-ai",
    "datasets",
    "pyyaml",
    "requests",
    "python-dotenv",
]
